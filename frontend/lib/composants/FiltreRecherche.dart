import 'package:flutter/material.dart';
import 'ChampRecherche.dart';

class FiltreRecherche extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.all(8),
      child: ChampRecherche(), 
    );
  }
}
